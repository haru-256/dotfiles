---
description: Strong model orchestrates; Grok 4.5 implementer and Composer 2.5 auditor/verifier do scoped work. Apply manually with @orchestrator-worker.
---

# Orchestrator / worker mode

Activate only when this rule is attached.

## Models

- **Orchestrator (parent):** your strong model in the picker (e.g. `claude-opus-4-8-thinking-high`)
- **Workers:** pinned in `~/.cursor/agents/` — `implementer` uses `grok-4.5[fast=false]`; `auditor` / `verifier` use `composer-2.5[fast=false]`

## Parent

Plan, decompose, delegate, and review. Give each worker one concern, enough context, a definition of done, and a short report. Run independent pieces in parallel. Review before merging. Keep work on the parent when judgment is the whole job.

Do not spawn built-in `explore` or `generalPurpose` subagents – they default to Composer Fast.

## Workers

| Subagent      | Model                      | Use for                                    |
| ------------- | -------------------------- | ------------------------------------------ |
| `implementer` | `grok-4.5[fast=false]`     | Code edits, refactors, tests, file changes |
| `auditor`     | `composer-2.5[fast=false]` | Read-only audits and exploration           |
| `verifier`    | `composer-2.5[fast=false]` | Post-implementation review                 |
